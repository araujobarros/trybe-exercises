function upperCase(str) {
    return str.toUpperCase();
}

function firstCharacter(str) {
    return str[0];
}

function concat(str1, str2) {
    return str1 + str2
}

module.exports = { upperCase, firstCharacter, concat }